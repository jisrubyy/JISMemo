<Window x:Class="JISMemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JISMemo"
        mc:Ignorable="d"
        Title="JISMemo" Height="600" Width="1000" WindowStartupLocation="CenterScreen"
        Icon="/Resources/JISMemo_Icon.ico">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <Border x:Name="TitleBar" Grid.Row="0" Background="#1E1E1E" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Margin="15,6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,4">
                    <Button x:Name="AddNoteButton" Content="➕ 새 멤모" Height="30" Padding="10,5" Click="AddNoteButton_Click" Style="{StaticResource SuccessButton}"/>
                    <Button x:Name="FindNotesButton" Content="🔍 멤모 찾기" Height="30" Padding="10,5" Margin="6,0,0,0" Click="FindNotesButton_Click" Style="{StaticResource AccentButton}"/>
                    <Button x:Name="OrganizeAllButton" Content="✨ 정리정렬" Height="30" Padding="10,5" Margin="6,0,0,0" Click="OrganizeAllButton_Click" Style="{StaticResource AccentButton}"/>
                    <Button x:Name="ResetSizeButton" Content="📊 크기 초기화" Height="30" Padding="10,5" Margin="6,0,0,0" Click="ResetSizeButton_Click" Style="{StaticResource PrimaryButton}"/>
                    <Button x:Name="ResetFontSizeButton" Content="🔤 폰트 초기화" Height="30" Padding="10,5" Margin="6,0,0,0" Click="ResetFontSizeButton_Click" Style="{StaticResource PrimaryButton}"/>
                    <Button x:Name="ArrangeNotesButton" Content="📐 멤모 정렬" Height="30" Padding="10,5" Margin="6,0,0,0" Click="ArrangeNotesButton_Click" Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,4,0,0">
                    <Button x:Name="SwitchUserButton" Content="👤 사용자 전환" Height="30" Padding="10,5" Click="SwitchUserButton_Click" Style="{StaticResource SystemButton}"/>
                    <Button x:Name="SettingsButton" Content="⚙️ 설정" Height="30" Padding="10,5" Margin="6,0,0,0" Click="SettingsButton_Click" Style="{StaticResource SystemButton}"/>
                    <Button x:Name="HelpButton" Content="❓ 도움말" Height="30" Padding="10,5" Margin="6,0,0,0" Click="HelpButton_Click" Style="{StaticResource SystemButton}"/>
                    <Button x:Name="CreditButton" Content="📜 제작자 정보" Height="30" Padding="10,5" Margin="6,0,0,0" Click="CreditButton_Click" Style="{StaticResource SystemButton}"/>
                    <Button x:Name="MinimizeButton" Content="— 최소화" Height="30" Padding="10,5" Margin="6,0,0,0" Click="MinimizeButton_Click" Style="{StaticResource SecondaryButton}"/>
                    <Button x:Name="ExitButton" Content="✖ 종료" Height="30" Padding="10,5" Margin="6,0,0,0" Click="ExitButton_Click" Style="{StaticResource DangerButton}"/>
                </StackPanel>
                
                <TextBlock x:Name="CurrentUserText" Text="" Foreground="#E0E0E0" VerticalAlignment="Center" 
                           HorizontalAlignment="Right" FontSize="13" Margin="0,0,15,0" FontWeight="Medium" Grid.RowSpan="2"/>
            </Grid>
        </Border>
        
        <ScrollViewer x:Name="MainScrollViewer" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" ScrollChanged="MainScrollViewer_ScrollChanged">
            <Canvas x:Name="NotesCanvas" Width="3000" Height="3000"/>
        </ScrollViewer>
        
        <Border Grid.Row="1" Width="200" Height="150" Background="#E0FFFFFF" BorderBrush="#333" BorderThickness="2" 
                HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" CornerRadius="4">
            <Canvas x:Name="MinimapCanvas" MouseLeftButtonDown="MinimapCanvas_MouseLeftButtonDown" MouseMove="MinimapCanvas_MouseMove" MouseLeftButtonUp="MinimapCanvas_MouseLeftButtonUp">
                <Rectangle x:Name="ViewportRect" Stroke="Red" StrokeThickness="2" Fill="#30FF0000"/>
            </Canvas>
        </Border>
        
        <Border Grid.Row="2" Background="#2C2C2C" BorderBrush="#444" BorderThickness="0,1,0,0">
            <Grid Margin="10,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="UIScaleLabel" Text="UI 크기:" Foreground="#E0E0E0" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Slider x:Name="UIScaleSlider" Width="150" Minimum="0.8" Maximum="1.5" Value="1.0" 
                            TickFrequency="0.1" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                            ValueChanged="UIScaleSlider_ValueChanged"/>
                    <TextBlock x:Name="UIScaleValue" Text="100%" Foreground="#E0E0E0" VerticalAlignment="Center" 
                               Margin="8,0,0,0" Width="45" TextAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
